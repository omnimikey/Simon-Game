/*
//SELECTS GREEN BUTTON
$(".green").on("click", function(event){
           green_beat.play();

            //adds fading effect
            $("#green").animate({
                opacity: '0.2',
              });
              $("#green").animate({
                opacity: '1',
              })
              //add to order sequence
              player.push(1);
            
            current_Press_Checker();

            if (order.length == array_tracker){
            generateSimon();
            sequenceRunner();
            player=[];
        }
});

//SELECTS RED BUTTON
$(".red").on("click", function(event){
    red_beat.play();

     //adds fading effect
     $("#red").animate({
         opacity: '0.2',
       });
       $("#red").animate({
         opacity: '1',
       })
       //add to order sequence
       player.push(2);

       current_Press_Checker();

       if (order.length == array_tracker){
        generateSimon();
        sequenceRunner();
        player=[];
        }       
});
*/
